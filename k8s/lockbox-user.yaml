apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaUser
metadata:
  name: '{{ "NAMESPACE" | env }}-lockbox'
  namespace: strimzi
  labels:
    strimzi.io/cluster: my-cluster
spec:
  authentication:
    type: tls
